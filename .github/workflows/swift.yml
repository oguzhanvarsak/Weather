name: Swift
on:
   push:
    branches-ignore:
          - master
   pull_request:
    branches:
         - master
         - develop
jobs:
   test:
     runs-on: macOS-latest
     steps:
       - uses: actions/checkout@master
       - name: Running Tests
          run: bundle exec fastlane tests
